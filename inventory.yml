all:
  hosts:
    # Must match Pulumi instances (see Pulumi config cloud-sandbox:instances)
    pierre.training.crafteo.io:
  vars:
    ansible_ssh_user: root
    ansible_ssh_private_key_file: "{{ playbook_dir }}/.ssh/crafteo"
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'